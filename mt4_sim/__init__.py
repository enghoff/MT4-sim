"""Isaac Sim model of the WLKATA MT4 arm, in the same coordinate frame the
real arm's control stack uses.

Geometry and joint limits come from the MT4 control repo (``mt4_jog``) rather
than being restated here, so the simulated arm cannot drift from the firmware.
See :mod:`mt4_sim.mt4_repo`.
"""
